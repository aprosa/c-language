#include <stdio.h>
int main()
{
int count = 0;
printf("\nEnter an integer");
scanf("%lld", &n);
while(n != 0)
{
n = n/10
n /= 10;
++count;
}
printf("\nNumber of digits");
scanf("%d", count);
return 0;
}
