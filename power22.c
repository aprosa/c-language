#include <stdio.h>
int main()
{
int num;
int temp,m,flag;
printf("\nenter the integer number");
scanf("%d",&num);
temp=num;
flags=0;
while(tempnum%2=0)
{
if(tempn%2!=0)
{
flag=1;
break;
}
tempnum=tempnum/2;
if(flag==0)
printf("%d",is a number that is power of z",num);
else
printf("%d is not the power of 2);
return 0;
}
}
