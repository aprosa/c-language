#include <stdio.h>
int main()
 {
int number, sums,i,array[10];
scanf("%d", &number);
for (i = 0; i < number; i++)
{
scanf("%d", &array);
sums = sums + array[c];
}
printf("Sum = %d\n",sums);
return 0;
}
