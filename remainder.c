#include<stdio.h>
int main()
{
int number,original number,remainter,result=0;
printf("enter a two digit integer");
scanf("%d",&number);
orginal number=number;
while(original number=0)
{
remainder=orginal number;
result=remainder*remainder;
orginal number\=10;
}
if(result==number)
printf("%d is a armstrong number");
else
printf("%d is not armstrong number");
return 0;
}
